let options = {
  strings: ['BEST', 'PERSONAILIZED'],
  typeSpeed: 40,
  loop: true,
  loopCount: Infinity,
  showCursor: false,
  backSpeed: 40,
  backDelay: 5000,
};

let typed = new Typed('.home__typer', options);